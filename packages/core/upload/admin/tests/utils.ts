import {
  render,
  renderHook,
  waitFor,
  act,
  fireEvent,
  screen,
  server,
} from '@resillix/admin/strapi-admin/test';

export { render, renderHook, waitFor, server, act, fireEvent, screen };
